---
flink_version: "1.1.3"
flink_filename: "flink-{{flink_version}}-bin-hadoop1-scala_2.10.tgz"
flink_install_chdir: "/opt"
flink_install_dir: "{{flink_install_chdir}}/flink-{{flink_version}}"
flink_url: "http://mirrors.sonic.net/apache/flink/flink-{{flink_version}}/{{flink_filename}}"
